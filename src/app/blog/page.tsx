import * as React from 'react';
import RootLayout from '../layout';

export interface BlogPageProps {
}

export default function BlogPage (props: BlogPageProps) {
  return (
    <div>
      Blog Page
    </div>
  );
}

BlogPage.Layout = RootLayout;