import * as React from 'react';
import RootLayout from '../layout';

export interface WorksPageProps {
}

export default function WorksPage (props: WorksPageProps) {
  return (
    <div>
      Works Page
    </div>
  );
}


WorksPage.Layout = RootLayout;